import 'package:flutter/material.dart';

class ExternalServicesScreen extends StatelessWidget {
  const ExternalServicesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('ExternalServices'),
      ),
    );
  }
}
